import styled from "styled-components";

const EpisodeGrid = () => {
  return (
    <StyledEpisodeGrid>
      Episode Grid
    </StyledEpisodeGrid>
  )
}

const StyledEpisodeGrid = styled.section`

`;

export { EpisodeGrid }
